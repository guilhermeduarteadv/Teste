# Migrations

A partir da v37, o schema vive consolidado em `install/schema.sql`.
Migrations 001–065 foram aplicadas, dump consolidado, e arquivadas em `_legacy/`
apenas como histórico. Para instalações novas, use `install/index.php`.
